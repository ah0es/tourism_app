import 'package:flutter/material.dart';

class AllFavoriteView extends StatelessWidget {
  const AllFavoriteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}